# github-public

Staging area — content of this folder is synced from
`~/Documents/github-public/` on the server. Drop files there and run the
sync (or ask the agent to "update the public repo") and they land here.

This folder is the repo-side half of that mapping; nothing is authored
directly in here.
